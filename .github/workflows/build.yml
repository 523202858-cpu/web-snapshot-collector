name: Build Android APK

on:
  workflow_dispatch:
  push:
    paths:
      - WebSnapshotCollector-Android-Project.zip
      - .github/workflows/build.yml

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Download source
        uses: actions/checkout@v4

      - name: Unpack project
        run: unzip -o WebSnapshotCollector-Android-Project.zip

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Android SDK
        uses: android-actions/setup-android@v3

      - name: Install Android SDK 35
        run: sdkmanager "platforms;android-35" "build-tools;35.0.0"

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: "8.9"

      - name: Build APK
        run: gradle -p WebSnapshotCollector :app:assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: WebSnapshotCollector-debug-apk
          path: WebSnapshotCollector/app/build/outputs/apk/debug/app-debug.apk
